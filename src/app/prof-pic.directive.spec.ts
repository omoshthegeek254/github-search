import { ProfPicDirective } from './prof-pic.directive';

describe('ProfPicDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfPicDirective();
    expect(directive).toBeTruthy();
  });
});
